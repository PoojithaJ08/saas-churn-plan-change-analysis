saas_churn:
  target: dev
  outputs:
    dev:
      type: postgres
      host: localhost
      port: 5432
      user: churn_user
      password: churn_pass
      dbname: churn_analytics
      schema: public
      threads: 4

# Copy this file to ~/.dbt/profiles.yml
# Credentials match docker-compose.yml — no changes needed for local dev.
# Never commit profiles.yml — it's in .gitignore.
