name: 'saas_churn'
version: '1.0.0'
config-version: 2

profile: 'saas_churn'

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  saas_churn:
    staging:
      +materialized: view
      +schema: staging
    marts:
      +materialized: table
      +schema: marts
